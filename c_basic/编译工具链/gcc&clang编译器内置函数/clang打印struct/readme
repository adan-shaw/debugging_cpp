借用clang 编译器的__builtin_dump_struct() 内建函数, 可以轻松打印任何复杂的结构体,
但遗憾的是: 
	这种方法只能一个函数打印一种结构体!!
	由于gcc 和clang 编译器互不兼容, 
	而且__builtin_dump_struct() 编译器内建函数, 并不是glibc / llvm 编译环境中的共享库API,
	而是clang 编译器的语法分析内建函数, 跟#define 宏定义差不多的, 具体实现方式由clang 自己实现, 这个细节不详细展开;

	所以最终:
		跨编译器解析编译, 最终只能以共享链接库的形式进行共享;
		而且只能是: 一个函数打印一种结构体!!
		只能打印结构体, 不能打印类;
