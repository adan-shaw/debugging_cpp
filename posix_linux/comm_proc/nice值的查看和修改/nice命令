nice 命令:
	用来设置一个新进程以<什么优先级>去启动一个进程(nice命令需要root 权限),
	nice 命令不能单独运行, 需要指定进程运行:
		nice # 默认参数, 只能查看: 当前terminal进程的优先值

	"nice -n"的使用格式:
		nice -n <优先级number> <./path/.exe or /bin/.exe>
		nice -n -21 xfce4-terminal # 只有再建一个终端, 你才能查看执行结果

		默认情况下, 进程以0的优先级启动. 
		进程优先级值的范围从-20到19. 值越低, 优先级越高.(不能超出范围)
