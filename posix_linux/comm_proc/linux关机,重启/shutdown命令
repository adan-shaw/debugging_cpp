shutdown 命令:
	(shutdown 命令默认是一分钟后自动关机)

	shutdown -h now   # 立马关机
	shutdown -h 20:25 # 系统会在今天20:25关机
	shutdown -h +10   # 十分钟后关机

	shutdown -r now   # 系统立马重启
	shutdown -r +10   # 系统十分钟后重启

	shutdown -c       # 取消关机指令

	(部分系统如ubuntu 等傻逼系统, 直接关机比较危险. 
	 服务器版本关机会触发一些数据保护机制, 你不知道怎样恢复就会导致可视化desktop 崩溃)
