互联网:
	mprotect()					系统调用修改'一块虚拟内存区域'上的属性;
	mlock(),mlockall()	系统调用将'一块虚拟内存区域'锁进物理内存, 从而防止它被交换出去;
	mincore()						系统调用让一个进程能够确定'一块虚拟内存区域'中的分页是否驻留在物理内存中;
	madvise()						系统调用让一个进程能够将其对'虚拟内存区域的使用模式'报告给内核;



my:
	madvise():					调整虚拟内存区域的'内核映射模式'(全局操作);
	mincore():					控制具体'哪一块虚拟内存区域分页'能驻留物理内存中;
	mlock(),mlockall():	将'某一块虚拟内存区域分页'锁进物理内存, 禁止将它交换出去;
	mprotect():					改变mmap 映射属性(读写属性,共有私有等属性)
