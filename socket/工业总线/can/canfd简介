canfd 也绝对是传输不了摄像头数据的,
应该是摄像头分析计算,得出文字数据元之后,
直接将计算结果,通过canfd 传输到'传感器融合系统'中,进行下一步人工智能操作;

目前'传感器融合系统'还没有机器学习框架? (bullshit)
单靠跑算法,果然是小机器,估计后期有大型芯片之后,才会将算法移植到机器学习框架上面;

这种公司现在只是在积累基础算法而已,还要耗很久的,这样砸钱,肯定是低薪资,耗很久的;





CANfd,CANoe,CANalyzer,CANape的区别:
	CANfd是一种CAN 协议的升级版协议;

	CANoe是Vector公司开发的一款总线开发测试工具,具备分析,仿真,测试,记录,诊断等多项功能,是一款软件;

	CANalyzer是一款网络分析工具,CANalyzer是CANoe的功能子集,是缩减版的CANoe,
	CANalyzer没有测试功能,不能作为测试工具去应用,只有记录,分析功能。

	CANape是一款ECU标定工具,可以对ECU的参数进行标定,测量分析,它与CANoe在功能上的差别还是比较大的;
	当然,CANoe利用XCP插件也可以做标定,但不能完全替代CANape;

	ps:
		CANoe,CANalyzer,CANape都需要搭配Vector的硬件设备来使用,如VN1600系列,VN8900系等等硬件接口卡和板卡;





关于CAN和CANFD物理层和数据链路层的对比分析:
	1,速率
		CAN的速率不可变,最大为1Mbps;
		CANFD的速率可变,最大为8Mbps,总裁比特率为1Mbps,与CAN一样;
		所以说,CANFD可变的速率都表现在数据场;

	2,数据长度
		CAN一帧数据长8字节,CANFD一帧数据长度最长为64字节;

	3,帧格式
		CANFD新增了FDF,BRS,ESI位;
		FDF:FDF为隐性电平,表示是CAN报文,FDF为显性电平,表示是CANFD报文;
		BRS:隐性表示转换为可变速率,显示表示转换为不可变速率;
		ESI:隐性表示发送节点处于被动错误状态,显性表示处于主动错误状态;

	4,CRC
		CAN与CANFD的CRC的计算方式不同;

	5,DLC的计算方式
		CANFD采用新的DLC的计算方式,在0-8位使用线性规则,在9-64位使用非线性规则;

	6,安全CAN总线
		如最近的CAN黑客攻击所示,经典CAN容易受到攻击;
		如果黑客能够访问CAN总线(例如,无线),则可能会关闭关键功能;
		带宽更高时,CANFD可以通过安全车载通信(SecOC)模块进行身份验证,这可能成为CANFD的核心理由;

	总结:
		CANFD具有更高的可调传输速率,更精确的CRC校验公式;
		CANFD的CAN的升级版,在物理层不变,只是在数据链路层发生变化;
