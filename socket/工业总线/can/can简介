CAN收发器下属线路, 使用双线串行实时;
CAN收发器与CAN域控制器, 使用双线串行异步, 符合uart通信标准;

布线格局不一样, 导致线路线速不一样;
协议标准不一样, 通信报文也不一样, 但双线的can 大同小异;

单线的can, 差异可能只是去掉时钟线;



can控制器 & can接收器, 都采用同一套协议;
can接收器负责将can控制器发来的信号, 翻译成线路信号;
can接收器负责将线路信号, 根据tcp/ip协议, 封装成数据包, 发送给can控制器;
