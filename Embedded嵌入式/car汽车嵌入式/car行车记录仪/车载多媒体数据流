车载多媒体数据流分析:
	* 坐舱音乐/电影, 数据流走向是: disk to qnx 子虚拟机(一般是android/linux 软件);
	* 行车记录仪, 数据流走向是: camera to disk, 独立线路, 独立系统, 不属于车载系统的一部分(一般是android/linux 软件);
	* 自动驾驶摄像头, 数据流走向是: camera to qnx 子虚拟机(一般是linux 软件);



多媒体数据流, 都不会走SomeIP/DDS 线路(因为数据流太大了);

播放音乐/电影, 一般在android-qnx子虚拟机中进行;

自动驾驶摄像头, 一般是从QNX 驱动层, 直接转发数据到linux-qnx 子虚拟机, 进行人工智能算法校验;

如果遇到行车记录仪, 应该是独立线路, 独立系统的, 不会经过QNX, 应该是独立开来, 免得占用网络带宽/系统资源;





自动驾驶摄像头会根据车厂的要求:
	导出指定分辨率, 帧率的码流, 直接输送到视觉计算算法层;
	(摄像头驱动是qnx 驱动; 视觉计算算法层是linux-qnx 子主机)

	如果有视频流处理的需求, 那就通过硬件进行转码, 有专门进行硬件转码的模块, 主控cpu 直接获取转码结果即可;
	(一般是linux 硬件解码/转码/... 等硬件加速, nvidia 方案)

	总之, 车载系统一般不会进行转码, 而是直接转发多媒体数据流到视觉计算算法层;








