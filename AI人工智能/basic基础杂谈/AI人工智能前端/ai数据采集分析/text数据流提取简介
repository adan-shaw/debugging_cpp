text检索,提取支撑技术(略):
	这个方向的支撑技术省略, 数据提取机器简单, 就字符串匹配, 
	strstr() 匹配子字符串即可, 出现即命中, 十分简单, 不用什么技术, 只需要做记录, 统计器即可;

	反而关键的变成:
		你需要提取哪些文字, 这些文字如何组合, 这倒是成为了难点;

	声音AI, 也有类似的难点, 声音AI 识别, 其实也不难, 
	关键是字词组合的匹配度, 意思识别能力的问题, 这倒成为了难点;



	所以, 这个text数据源提取支撑技术, 忽略, 如果有需要, 你可以再实现, 集中精力做视觉计算!!



	text数据提取很简单, 提取统计倒很难, 
	text有效数据收集, 也很难,
	text有效组合数据命中, 也很难,
	难点不一样, 区分对待;
