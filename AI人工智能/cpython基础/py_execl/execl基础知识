1.excel中有xls和xlsx两种格式, 它们之间的区别是:
	文件格式的不同:
		xls是一个特有的二进制格式, 其核心结构是复合文档类型的结构;
		xlsx的核心结构是XML类型的结构, 采用的是基于XML的压缩方式, 使其占用的空间更小(xlsx中最后一个x, 即xml 的意思)

	版本年代的不同:
		xls是excel2003及以前版本生成的文件格式,
		xlsx是excel2007及以后版本生成的文件格式;

	兼容性的不同;
		xlsx格式是向下兼容的, 可兼容xls格式;
		但xls格式的兼容性较差;





2.什么是excel 的工作簿、工作表、行、列、单元格?
	* workbook工作簿(多个二维表)
		一个xls/xlsx, 就是一个workbook工作簿;

	* sheet工作表(二维表, 索引数字从0 开始, 自动表名从Sheet1 开始)
		一个xls/xlsx, 里面可以有n个sheet工作表;

	* row行
		一个sheet工作表, 里面可以有n 行;

	* column列
		一个sheet工作表, 里面可以有n 列;

	* cell单元格
		一个数据单元格, 就是cell; cell 的数据类型通常为: string/date/number/图片or二进制数据(唯一不能排序的)
